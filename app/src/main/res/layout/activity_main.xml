<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_navy">

    <!-- Header con botón QR -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Logo Spatium 10 Aniversario centrado -->
        <ImageView
            android:id="@+id/logoSpatium"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:layout_marginStart="80dp"
            android:layout_marginEnd="80dp"
            android:scaleType="fitCenter"
            android:contentDescription="Spatium 10 Aniversario"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Botón QR en esquina superior derecha -->
        <FrameLayout
            android:id="@+id/btnScan"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/qr_button_background"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="center"
                android:src="@drawable/qr_icon_gold"
                android:contentDescription="Escanear QR" />
        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Contenedor central con icono, texto y marco de escaneo -->
    <LinearLayout
        android:id="@+id/centerContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/header">

        <!-- Icono QR central grande -->
        <ImageView
            android:id="@+id/qrIconCenter"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/qr_icon_gold"
            android:contentDescription="QR Code Icon"
            android:layout_marginBottom="32dp" />

        <!-- Texto principal elegante -->
        <TextView
            android:id="@+id/mainText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Escanee el QR para\nver la información\ndel evento"
            android:textColor="@color/gold"
            android:textSize="32sp"
            android:textStyle="normal"
            android:fontFamily="serif"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="48dp" />

        <!-- Marco de escaneo dorado -->
        <FrameLayout
            android:id="@+id/scanFrame"
            android:layout_width="280dp"
            android:layout_height="280dp"
            android:background="@drawable/qr_scan_frame"
            android:visibility="gone">

            <!-- Línea de escaneo animada -->
            <View
                android:id="@+id/scanLine"
                android:layout_width="match_parent"
                android:layout_height="3dp"
                android:background="@color/gold"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:alpha="0.8" />
        </FrameLayout>

        <!-- Texto de estado de escaneo -->
        <TextView
            android:id="@+id/scanStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="@color/white_80"
            android:textSize="16sp"
            android:layout_marginTop="24dp"
            android:visibility="gone" />

        <!-- Botón Tour (oculto por defecto, se mantiene para compatibilidad) -->
        <LinearLayout
            android:id="@+id/btnTour"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:orientation="vertical" />

        <!-- Botones de prueba ocultos -->
        <LinearLayout
            android:id="@+id/btnSeqPlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/btnSeqTest"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/permPanel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
